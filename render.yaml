services:
  - type: web
    name: bachar-game-lab
    env: static
    staticPublishPath: public
    buildCommand: mkdir -p public && cp -r games games.json index.html public/
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
