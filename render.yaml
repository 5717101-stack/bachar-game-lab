services:
  - type: web
    name: bachar-game-lab
    env: static
    staticPublishPath: ./
    buildCommand: "true"
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
